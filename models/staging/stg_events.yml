version: 2

models:
  - name: stg_events
    columns:
      - name: user_pseudo_id
        tests:
          - not_null

  - name: stg_events_intraday
    columns:
      - name: user_pseudo_id
        tests:
          - not_null
          