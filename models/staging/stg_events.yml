version: 2

models:
  - name: stg_events
    description: staged events data from HDB web game
    columns:
      - name: user_pseudo_id
        description: unique id of each device
        tests:
          - not_null

  - name: stg_events_intraday
    description: staged events data from HDB 
    columns:
      - name: user_pseudo_id
        description: unique id of each device
        tests:
          - not_null
          